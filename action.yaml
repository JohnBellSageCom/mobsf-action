name: 'GitHub Action for MobSF'
author: 'John Bell'
description: 'Wraps the MobSF docker to enable workflows on GitHub Actions'
branding:
  icon: 'package'
  color: 'gray-dark'
runs:
  using: 'docker'
  image: 'Dockerfile'
outputs:
  json:
    description: 'The created MobSF report in json format'
